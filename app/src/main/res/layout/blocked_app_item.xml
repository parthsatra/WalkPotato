<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@+id/remove_app_btn"
        android:layout_marginEnd="20dp">

        <ImageView
            android:id="@+id/appImageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical" />


        <TextView
            android:id="@+id/app_name_string"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|right"
            android:layout_marginLeft="5dp"
            android:textColor="@android:color/black"
            android:textSize="32sp" />

        </LinearLayout>

    <ImageButton
        android:id="@+id/remove_app_btn"
        android:tint="@android:color/white"
        android:background="@drawable/oval_ripple"
        android:elevation="@dimen/elevation_high"
        android:layout_width="@dimen/round_small_button_diameter"
        android:layout_height="@dimen/round_small_button_diameter"
        android:adjustViewBounds="true"
        android:padding="4dp"
        android:scaleType="fitCenter"
        android:layout_gravity="bottom"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:src="@android:drawable/ic_delete"/>

</RelativeLayout>
